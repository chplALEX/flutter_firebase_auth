name: flutter_firebase
description: A new test Flutter and Firebase project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.6.6
  firebase_app_check: ^0.3.2+5
  firebase_auth: ^5.5.2
  firebase_core: ^3.13.0
  flutter_facebook_auth: ^7.1.1
  google_sign_in: ^6.2.2
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
